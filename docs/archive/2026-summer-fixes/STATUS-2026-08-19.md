# Habit Club — Единый статус / 2026-08-19

> **Дата среза:** 2026-08-19. **Snapshot** по `git log`, проде, и `Pravki-business-logic-recon-2026-08-18.md`.
> Этот документ — **точка входа** для понимания «где мы и что делать дальше».
> Старые доки (Pravki-*, sse+redis.md, TZ_kharakteristiki_personazha.md и т.д.) **сохранены** как история решений.
> Только этот файл синхронизирован с реальностью на 2026-08-19.

---

## 0. TL;DR

- **Прод:** бэкенд + worker + бот на `c6647b7`, 7 контейнеров Up, `penalty_service.py` md5 = HEAD (синхронизировано). Frontend 5 дней без рестарта.
- **Последние 27 дней (2026-07-23 → 2026-08-19):** 8 задеплоенных серий, ~14 коммитов, включая manual-catch Z-1/Z-2/Z-3 (отключение авто-списания, ручная поимка только).
- **2 локальных docs-коммита** (`d3972bf` + `2dd596c`) **не запушены** — sync с origin потерян на 2 коммита назад. Push нужен **отдельным явным "ок"** (правило из AGENTS.md).
- **Что горит прямо сейчас:** ничего финансово критичного. **Единственная** оставшаяся «бьёт деньги» дыра — `#17` (`apply_catch` deposit=0 без WAIVED-маркера), 5 строк кода. Без неё регрессии копятся, но реальных пользователей и денег в проде пока **нет** (2 тест-юзера, 0 транзакций).
- **Большой техдолг** (28 находок в recon'е, +4 TD от wip-other-task) — но **всё не блокирует**, кроме `#17`. Sprint 1 = только `#17`. Sprint 2-4 = не срочно.
- **Следующие действия (план ниже в §5):** `git push` (по "ок") → опционально: fix `#17` → идти по soft-launch чеклисту из `docs/09-prod-readiness.md §2.1-2.3`.

---

## 1. Где мы сейчас

### 1.1 Git-синхронизация (на 2026-08-19, 18:29 МСК)

| Точка | SHA | Комментарий |
|---|---|---|
| `HEAD` (локально) | `2dd596c` | 2 docs-коммита сегодня сверху |
| `feature/qa-batch-2026-08-14` | `2dd596c` | указатель ветки |
| `origin/feature/qa-batch-2026-08-14` | `c6647b7` | **отстаёт на 2 docs-коммита** — нужен `git push` |
| `habit-backend` контейнер | `c6647b7` | `penalty_service.py` md5 = `c61bce8f…` = HEAD (c6647b7) ✅ |
| `habit-worker` | `c6647b7` | healthy |
| `habit-bot` | `c6647b7` | 43 часа uptime |
| `habit-frontend` | nginx 1.27 | 5 дней uptime (volume-mount workaround) |
| `habit-postgres` / `habit-redis` / `habit-pgweb` | 4 недели | healthy |

**Локальные коммиты поверх origin (нужен push):**
```
2dd596c docs(recon): apply Z-1/Z-2/Z-3 status changes (snapshot 2026-08-19)
d3972bf docs: update §6.1/§6.2 audit status, add business-logic-recon
```

### 1.2 Worktrees

```
/Users/dmitriy/Downloads/Privichki                 (main, HEAD 2dd596c)
/Users/dmitriy/Downloads/Privichki/.kilo/worktrees/fringe-aurora  (отдельная задача, не трогаем)
```

Worktree `/tmp/privichki-impact-2026-08-18` удалён 2026-08-19 (архивирован в `/tmp/wip-other-task-final-archive-20260819-122724.patch`).

### 1.3 БД на проде (snapshot 2026-08-09, до Z-серии)

- 2 users (тестовые)
- 3 habits (Пробежка, Планка, Чтение)
- 3 memberships
- 4 transactions

> **Не верифицировал свежим запросом к БД** — могло измениться. Проверить: `ssh privichki-prod "docker exec habit-postgres psql -U prideclub -c 'SELECT count(*) FROM users; SELECT count(*) FROM habits; ...'"`.

---

## 2. Что сделано за 27 дней (2026-07-23 → 2026-08-19)

### 2.1 Production-серии (на проде)

| # | Серия | HEAD-коммиты | Дата | Что |
|---|---|---|---|---|
| 1 | Lint-zero | `7a39fc1` | 2026-07-23 | ruff clean, `Annotated[..., Depends(...)]` DI, импорты |
| 2 | Topic-scoped чек-ины | `84f2640` + миграции 010/011 | 2026-07-23 | 3 топика (checkin/notifications/chat), фильтрация по `message_thread_id` |
| 3 | Multi-proof_types | `a27b220` + миграция 012 | 2026-07-23 | клубы принимают 1-3 типа чек-ина (video_note/photo/text) |
| 4 | Bot pre-filter proof_types | `b6d902c` | 2026-07-23 | PR №9: бот проверяет тип/дубликат ДО backend |
| 5 | SSE через Redis Streams | `c836542`..`d30832a` (merge `0c9a7b8`) | 2026-08-04 | 6-step, JWT-токен, XREAD BLOCK, `useTodayStream` hook |
| 6 | Pravki-deposit-sse PR #1 | `ac6951f` + миграции 014a/014b | 2026-08-08 | `users.deposit_balance`, `recompute_pause_status` |
| 7 | Pravki-deposit-sse PR #2 | `9736b5b` | 2026-08-08 | `useWallet`, `TopUpModal`, `WalletClubOut.can_checkin` |
| 8 | Pravki-subscribe-and-join | `b51eb90` + `b98cab0` | 2026-08-08 | `POST /api/v1/payments/subscribe`, `subscribe_and_join` (3a/3b/3c) |
| 9 | LEFT/PAUSED bypass fix | `ae6bd07` | 2026-08-08 | `join` всегда проверяет депозит |
| 10 | Pravki-bug-fixes Z-19 | `497d01d` + `564b8db` + миграция 015 | 2026-08-09 | joiner-late protection (3 уровня defense-in-depth) |
| 11 | Frontend image workaround | `4a390e1` | 2026-08-10 | nginx volume-mount (overlay-конфликт fix) |
| 12 | Pravki §Z-22 pre-filter | `b4f8974`..`b4cc923` (7 коммитов) | 2026-08-10 | 4 дырки паттерна async-reject-after-accepted |
| 13 | Pravki-no-deposit-waived-marker | `5bfeec7` + `9c32d6f` | 2026-08-17 | `WAIVED_UNABLE_TO_PAY` для `PAUSED` юзеров, idempotency по всем reason |
| 14 | Pravki-subscription-2026-08-17 | `9c96320` + `3240b83` + `9842d4e` + `abde0d3` | 2026-08-17 | smart renew, 4 уровня defense-in-depth, `SubscriptionBadge` 3-state |
| 15 | **Pravki-manual-catch Z-1/Z-2/Z-3** | `f4eb243` + `48210e9` + `1b1d325` + `3b81327` | 2026-08-18 | **отключение авто-списания**: cron = housekeeping, `apply_window_expired` deprecated, окно ловли = `is_within_catch_window` |

### 2.2 Docs-серии (тоже закоммичены)

| # | Коммит | Что |
|---|---|---|
| 1 | `e2a3598` | `docs(manual-catch): sync docs with step-3 no-auto-charge model` |
| 2 | `c6647b7` | `docs(prod-readiness): clarify deploy-not-executed and add cleanup TODO` |
| 3 | `d3972bf` | `docs: update §6.1/§6.2 audit status, add business-logic-recon` (локально, **не запушено**) |
| 4 | `2dd596c` | `docs(recon): apply Z-1/Z-2/Z-3 status changes` (локально, **не запушено**) |

### 2.3 Что manual-catch Z-1/Z-2/Z-3 изменил (главное за неделю)

- **Авто-штрафов больше нет.** Штрафы только через ручную поимку (`apply_catch`).
- `apply_window_expired` и `mark_waived_unable_to_pay` → **safe no-op** (`logger.warning("deprecated_auto_penalty_skipped")` + `return None`).
- `close_catch_window` → **housekeeping** (только `Checkin(status='missed')` + `recompute_pause_status`), **0 `Penalty` / 0 `Transaction` / 0 deposit-движений**.
- Новое: `Habit.catch_window_end(club_date)` + `is_within_catch_window(now_utc, club_date)` (per-habit, не «checkin + 1ч»).
- Новое: `CatchWindowClosedError(422)` в `apply_catch` после границы окна ловли.
- `CATCH_WINDOW_BUFFER_HOURS=2` (был 1, теперь 2).
- `WINDOW_CLOSED_NO_CATCH` и `WAIVED_UNABLE_TO_PAY` как авто-пути — **deprecated** (в проде 0 строк с этими reason).

### 2.4 Тесты на 2026-08-10 (после Z-19 + Pravki-subscription)

| Пакет | Passed | Замечание |
|---|---|---|
| `apps/backend/tests` | **384** + 11 failed (9 pre-existing admin_habits_api, 1 user_photo_endpoint mock, 1 test_migrations) | не запускаются на проде |
| `apps/worker/tests` | **77** + 9 failed | pre-existing, не связано с этой сессией |
| `apps/bot/tests` | **40** | все зелёные |
| `apps/frontend/tests` | **68** | vitest |

Точные цифры после Z-1/Z-2/Z-3 не верифицировал — но **ни одной регрессии** серии Z не вносили (`rebase test_worker_cron_chain.py` на 1020 строк + 11 новых).

---

## 3. Что НЕ работает / известные проблемы (из recon'а)

Источник: `Pravki-business-logic-recon-2026-08-18.md` (986 строк, 28 находок + Sprint 1-4 план). **Snapshot 2026-08-19 уже учтён** — пункты #3 и #16 помечены как закрытые Z-серией.

### 3.1 Единственная «бьёт деньги» дыра 🔴

| # | Что | Где | Сложность |
|---|---|---|---|
| **#17** | `apply_catch` для `deposit=0` бросает `PenaltyAlreadyProcessedError("deposit_exhausted")` **БЕЗ** записи WAIVED-маркера | `apps/backend/app/services/penalty_service.py:171-177` | **5 строк** + e2e-тест |

После topup `apply_catch` спишет деньги за прошлый день, который был waived. Прямая финансовая потеря. Но: **реальных пользователей нет**, **транзакций 0** → не стреляет сегодня. Стрельнёт на первом реальном юзере.

### 3.2 Не стреляют сейчас, но станут актуальны на росте 🟠

| # | Что | Файл | Сложность | Почему не сейчас |
|---|---|---|---|---|
| **#1** | `apply_catch_bonus` не вызывается — лидерборд «Охотники» пуст | `celery_producer.py:21-35` + `process_penalty.py` | 2 строки + e2e-тест через broker | нет юзеров, ожидающих бонусы |
| **#2** | Сезоны мёртвый код — `close_season` раздаёт 0₽ | `season_service.py:45` + admin endpoint нет | 5+ файлов + admin endpoint | сезонов нет в проде |
| **#4** | Forward в worker defense-in-depth | `internal_checkins.py:48` + `process_checkin.py:214-220` | 1-2 строки | бот prefilter защищает, service-token в секрете |
| **#6** | Canonical priority в worker (combo caught_today+paused) | `checkin_service.py:122-241` | reorder 10 строк | bypass-only, бот prefilter правильно отдаёт |
| **#11** | `Habit.prize_pool` без изоляции по Season | `models/habit.py:56` + `models/season.py:28` | связано с #2 | нет сезонов |

### 3.3 Техдолг 🟡 (не блокирует, не бьёт деньги)

| # | Что | Файл |
|---|---|---|
| #7 | `date.today()` в `subscribe_and_join` → UTC | `membership_service.py:318` |
| #8 | `apply_catch` habit-mismatch | `internal_penalties.py:34-45` |
| #9 | Catch rate-limit fail-mode (Redis crash mid-INCR) | `process_penalty.py:243-274` |
| #10 | SSE broadcasts после commit | `members.py:271-352` |
| #12 | `stale_message` window дубль | `proof_validator.py:60-62` |
| #13 | Окна через полночь (ночные клубы) | `habit.py:113-127` |
| #14 | `refresh(violator)` под локом | `penalty_service.py:123` |
| #15 | `effective_deposit` race | `membership_service.py:361-378` |
| #18 | join LEFT→ACTIVE без deposit-check | `membership_service.py:58-65` |
| #19 | leave без возврата депозита (док обещает возврат, кода нет) | `membership_service.py:138-145` |
| #20 | ФЗ-152: `current_user_db` upsert без `accepted_offer_at` | `users.py:33-56` |
| #22 | `suspicious_pairs.evaluate_after_catch` мёртвый код | `suspicious_pairs_service.py:32-82` |
| #23 | IntegrityError в catch race теряет SSE | `process_penalty.py:210-213` |
| #24-#28 | Архитектурные мелочи (archived clubs, SSE TTL, SeasonStats) | various |

### 3.4 Tech-debt TD-1..TD-4 (admin wip-other-task, не зависит от Z)

| # | Что | Файл |
|---|---|---|
| TD-1 | Бизнес-логика в роуте `list_available_chats` (~200 строк) | `admin/v1/habits.py` |
| TD-2 | Нет rate-limit на Bot API (`_verify_chats_via_telegram`, лимит 30 req/sec) | `admin/v1/habits.py` |
| TD-3 | Прямое обращение `service._habit_repo.X` из роута | `admin/v1/habits.py` |
| TD-4 | Нет тестов для `_verify_chats_via_telegram` + `chat_member.py` | `backend/tests/`, `bot/tests/` |

### 3.5 Cleanup TODO (мелкое, из prod-readiness §3)

🟡 9 устаревших production-комментариев ссылаются на старую auto-charge модель (файлы: `core/exceptions.py:127`, `repositories/checkin_repository.py:119,125`, `repositories/habit_repository.py:167`, `repositories/penalty_repository.py:76,116`, `schemas/__init__.py:167`, `services/checkin_service.py:107,188,192`, `services/penalty_service.py:114,169,174-179,237,309`, `api/v1/internal_bot.py:330,406`). Не влияют на поведение. **Отдельная задача.**

### 3.6 Known limitations (снапшот 2026-08-19)

- `ValueError` на naive datetime в `close_catch_window._close_for_habit` — defensive guard, production path всегда tz-aware. Sentry = no-op, наблюдаемость = `docker logs habit-worker | grep ERROR`.
- `close_catch_window` cron = `crontab(minute=5)`. Per-habit-beat через `apply_async(eta=...)` — отдельная задача, **не блокирует** (текущий cron = housekeeping, no-op внутри окна).
- `habit-frontend` volume-mount workaround (без `build:`) — переживает recreate, но **первопричина overlay-конфликта не закрыта**. См. AGENT_BOOTSTRAP §9.
- `habit-bot` plain text logging, не structlog (как backend/worker).
- `chat_id` vs `habit_id` контракт в `internal_payments.py` сломан (ждёт 422). Endpoint мёртвый, бот не вызывает.

### 3.7 Frontend-блокер (отдельная большая тема)

**Frontend страницы НЕ подключены к backend API** (см. `docs/09-prod-readiness.md §2.3 Шаг 8`). 3-5 дней работы фронт-разработчика:
- Marketplace → `/api/v1/habits`
- Today → `/api/v1/checkins/today`
- Members → `/api/v1/members/*`
- Balance → `/api/v1/balance`
- Leaderboard → `/api/v1/leaderboard/*`
- Profile → `/api/v1/users/me`
- Onboarding → `/api/v1/habits/join`

API endpoints существуют, нужно писать хуки + UI.

---

## 4. Что НЕ блокирует (но висит как "отложено")

Из `docs/09-prod-readiness.md §2.2-2.3`:
- Бэкапы PostgreSQL (нет дешёвого S3 в РФ)
- Sentry DSN (OAuth GitHub неудобен)
- Перенос БД в Selectel managed (нужен для ФЗ-152)
- Rate-limit в nginx (есть в backend)
- Prometheus + Alertmanager
- Load testing (1000 users)
- ФЗ-152 compliance (политика + удаление аккаунта)
- Frontend API integration (см. §3.7)

Всё это — задачи для soft-launch / широкого запуска, **не сейчас**.

---

## 5. План по приоритету

### 5.1 Немедленно (сегодня, 2026-08-19)

| # | Действие | Блокирующее условие | Время |
|---|---|---|---|
| **A** | `git push origin feature/qa-batch-2026-08-14` (залить 2 docs-коммита) | явное "ок" пользователя | 5 сек |
| **B** | Прочитать этот документ, подтвердить что картина верная | ничего | 10 мин |

**После "ок" на push:** HEAD = 2dd596c, все три точки (local / ветка / origin) синхронизируются. Можно планировать дальше.

### 5.2 Sprint "сейчас" (если решишь чинить #17)

| # | Действие | Сложность | Риск |
|---|---|---|---|
| **#17 fix** | `apply_catch` для deposit=0 пишет WAIVED-маркер | 5 строк + e2e-тест | низкий (логика как в `apply_window_expired` WAIVED-ветке) |
| **Cleanup TODO** | 9 устаревших production-комментариев | 1 файл / 1 файл | нет (только правка текста) |

**Не делаю по своей инициативе** (по правилу AGENTS.md). Скажешь — сделаю.

### 5.3 Sprint 2 (если/когда будут пользователи)

- **#1** `apply_catch_bonus` wiring (2 строки + e2e-тест через broker)
- **#4** forward в worker defense-in-depth (1-2 строки)
- **#6** canonical priority в worker (reorder 10 строк)

### 5.4 Sprint 3 (UX/ФЗ-152)

- **#13** окна через полночь
- **#7** `date.today()` → UTC
- **#20** ФЗ-152: user только после `/start` или `accept_offer`
- **#18** LEFT→ACTIVE с deposit-check
- **#19** leave + refund (или убрать обещание из доков)

### 5.5 Sprint 4 (MVP seasons, нужен product decision)

- **#2** + **#11** + **#28** — связаны. Admin endpoint, snapshot-логика, snapshot apply в `apply_catch`.

### 5.6 Архитектурный (не срочно)

- TD-1..TD-4 (admin wip-other-task hardening)
- Per-habit-beat cron (косметика расписания)
- Frontend image build (вернуть `build:` вместо volume-mount)
- Frontend bot structlog (логирование)

---

## 6. Куда смотреть (карта доков)

| Нужно | Файл | Размер |
|---|---|---|
| **Общий статус (этот файл)** | `STATUS-2026-08-19.md` | 250 строк |
| Аудит дыр + Sprint-план | `Pravki-business-logic-recon-2026-08-18.md` | 986 строк |
| История Pravki-серий | `Pravki.md` | 541 строка |
| Прочитать прода (деплой, сервер, SSH, .env) | `docs/AGENT_BOOTSTRAP.md` | 848 строк |
| Прочитать прода (что задеплоено, чеклист soft-launch) | `docs/09-prod-readiness.md` | 321 строка |
| Архитектура (слои, потоки, домены) | `docs/02-architecture.md` | 596 строк |
| Code standards (layered architecture, DI, антифрод) | `docs/04-code-standards.md` | 744 строки |
| Data model (таблицы, антифрод, идемпотентность) | `docs/06-data-model.md` | 695 строк |
| Деплой-процедура (rsync → build → up) | `docs/10-deploy.md` | 378 строк |
| Безопасность (auth, ФЗ-152, бэкапы) | `docs/07-security-and-ops.md` | 492 строки |
| Правила для AI-агентов | `AGENTS.md` + `.kilo/rules/PRIVICHKI.md` | 83 + N |
| Ритуал поддержания доков | `AGENTS.md §12` | — |
| Hard-блокеры (что НЕ делать) | `AGENTS.md §13` + PRIVICHKI.md §"Чего НЕ делать" | — |

| Серия фиксов (история) | Файл | Размер |
|---|---|---|
| Manual-catch Z-1/Z-2/Z-3 (2026-08-18) | `Pravki-manual-catch-step3-2026-08-18.md` | 375 строк |
| Deposit-SSE (2026-08-08) | `Pravki-deposit-sse.md` | 1561 строка (!!) |
| Subscribe-and-join (2026-08-08) | `Pravki-subscribe-and-join.md` | 1082 строки |
| No-deposit WAIVED-marker (2026-08-17) | `Pravki-no-deposit-waived-marker.md` | 357 строк |
| Subscription-2026-08-17 | `Pravki-subscription-2026-08-17.md` | 272 строки |

| Продуктовые доки (не менялись) | Файл | Размер |
|---|---|---|
| ТЗ персонажа + характеристики | `TZ_kharakteristiki_personazha.md` | 822 строки |
| Карта проекта (product view) | `prideclub_karta_proekta.md` | 194 строки |
| Финансовая механика (идея) | `4_finansovaya_mehanika_shtrafov_i_prizov.md` | 178 строк |
| Маркетинговые playbook'и | `1_kabinet_partnera_MVP.md`, `2_poisk_partnerov_instagram.md`, `3_zapusk_cherez_liderov_soobshestv_checklist.md` | ~400 строк |

| Тех-черновики (могут быть устаревшими) | Файл | Размер |
|---|---|---|
| sse+redis.md (старый черновик, дублирует SSE Steps) | `sse+redis.md` | 1129 строк |
| qa-checklist-simple.md | `qa-checklist-simple.md` | 154 строки |
| Архитектурное решение аватаров | `Архитектурное решение ... .md` | 117 строк |

> **Примечание:** `sse+redis.md` (1129 строк) — большой старый черновик, **вероятно дублирует** `Pravki-deposit-sse.md` и `docs/09-prod-readiness.md §1.1 row 19`. Не верифицировал. Если мешает — отдельная задача "почистить/архивировать".

---

## 7. Что не делать прямо сейчас (напоминание)

Из `docs/09-prod-readiness.md §7`:
- ❌ Не оптимизировать производительность до load-test
- ❌ Не рефакторить рабочий код без причины
- ❌ Не менять архитектуру
- ❌ Не переносить БД в РФ до soft-launch
- ❌ Не подключать Sentry если не хочешь OAuth

Из `AGENTS.md`:
- ❌ Не коммитить секреты, пароли, `.env`
- ❌ Не коммитить **приватные** SSH-ключи
- ❌ Не править `/app` на сервере напрямую
- ❌ Не `docker compose down` без ок
- ❌ Не коммитить от `Dim41g / ivanov1331d@gmail.com` (только Vegass / dmitriy@vegass.dev)
- ❌ Не пушить в `origin/main` без явного "ок"
- ❌ Не использовать `any` в TypeScript без обоснования
- ❌ Не логировать PII (`first_name`, `username`)
- ❌ Не делать "быстрых" изменений на сервере без плана в чате
- ❌ Не переписывать документацию целиком
- ❌ Не добавлять бизнес-логику в роуты

---

## 8. Сводный snapshot (для copy-paste в чат / отчёты)

> **Habit Club — STATUS 2026-08-19**
>
> **Прод:** `c6647b7` задеплоен (15 production-серий с 2026-07-23). 7 контейнеров Up, healthy. Frontend volume-mount workaround 5 дней без рестарта.
>
> **Git:** HEAD = `2dd596c` локально (2 docs-коммита сверху). origin отстаёт на 2 коммита — нужен push. Никаких `rebase`/`merge` в текущей сессии.
>
> **Финансовые дыры:** 0 активных. Единственная оставшаяся (`#17 apply_catch` deposit=0 без WAIVED) — 5 строк, не стреляет (0 транзакций в проде). Sprint 1 (2 крита после Z-серии) — технический долг.
>
> **Большой долг:** 28 находок в recon'е, +4 TD. Всё не блокирует. Самые актуальные после `#17`: #1 (apply_catch_bonus wiring), #4 (forward), #6 (canonical priority).
>
> **Следующие действия:**
> 1. `git push origin feature/qa-batch-2026-08-14` (по "ок")
> 2. Опционально: fix #17 (5 строк)
> 3. Frontend API integration (3-5 дней frontend-разработчика)
> 4. Soft-launch для 10-50 человек возможен СЕЙЧАС по бэкенду
> 5. До широкого запуска: managed PostgreSQL в Selectel + ФЗ-152 compliance

---

## 9. Контакты и ownership

| Зона | Ответственный | Статус |
|------|--------------|--------|
| Код (Python, SQL, инфра) | AI-ассистент (я) | ✅ |
| Домен + DNS | Дмитрий | ✅ `prideclub.fun` работает |
| Telegram bot setup | Дмитрий | ✅ `/setdomain` сделан |
| S3 для бэкапов | Дмитрий + я | ⏸ отложено |
| Selectel managed БД (переезд) | Дмитрий + я | ⏸ когда будут пользователи |
| Юридическое (ФЗ-152) | Дмитрий | ⏸ когда будет прод |
| Frontend | Дмитрий + frontend-разработчик | ⏸ 3-5 дней работы |
| Push в origin (2 docs-коммита) | AI-ассистент по "ок" | ⏸ ожидает |

---

## 10. Как пользоваться этим документом

- **Сейчас потерялся** — открой §0 (TL;DR) и §5.1 (что делать прямо сейчас).
- **Хочешь понять что задеплоено** — открой §2.1 (таблица production-серий) + `docs/09-prod-readiness.md §1.1` (29 пунктов с деталями).
- **Хочешь понять что не работает** — открой §3 (отфильтрованный по приоритету список из recon'а).
- **Хочешь взять задачу** — открой §5 (план по приоритету) → запись в recon'е → исходный код.
- **Хочешь разобраться как устроен проект** — открой §6 (карта доков) → `docs/AGENT_BOOTSTRAP.md` (848 строк, главный onboarding).

---

**Дата создания:** 2026-08-19
**Автор:** AI-ассистент по запросу Дмитрия (read-only разведка + синтез)
**Источники:** `git log`, `docs/09-prod-readiness.md` §1.1, `Pravki-business-logic-recon-2026-08-18.md`, `ssh privichki-prod docker ps`
**Не синхронизировано с:** `sse+redis.md` (1129 строк — старый черновик, не верифицировал актуальность).
**Не заменяет:** старые Pravki-* и `docs/*.md` доки — это **карта** к ним, не замена.
